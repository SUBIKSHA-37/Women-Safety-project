const swatches={shortest:'#D32F2F',balanced:'#FF9800',safest:'#00C853'};
export default function RouteOptions({routes,selectedId,onSelect}){if(!routes?.length)return null;return <div className="route-options"><p className="eyebrow">Choose your route</p>{routes.map(route=><button key={route.id} onClick={()=>onSelect(route.id)} className={'route-option '+(route.id===selectedId?'selected':'')}><i style={{background:swatches[route.id]}}/><span><b>{route.label}{route.id==='safest'&&' · Recommended'}</b><small>{(route.distance_m/1000).toFixed(1)} km · risk {route.average_edge_risk}</small></span></button>)}</div>}
