import { MapPin, Navigation, Search } from 'lucide-react';
const cities=['Chennai','Kallakurichi','Thanjavur','Coimbatore','Madurai'];
export default function RouteForm({form,setForm,onSubmit,loading}){const update=(key,value)=>setForm({...form,[key]:value}); return <form onSubmit={onSubmit} className="space-y-4"><div><label>Travel city</label><select value={form.city} onChange={e=>update('city',e.target.value)}>{cities.map(city=><option key={city}>{city}</option>)}</select></div><div><label>Starting point</label><div className="input-wrap"><MapPin/><input value={form.source} onChange={e=>update('source',e.target.value)} placeholder="e.g. Anna Nagar" required/></div></div><div><label>Destination</label><div className="input-wrap"><Navigation/><input value={form.destination} onChange={e=>update('destination',e.target.value)} placeholder="Where are you going?" required/></div></div><button disabled={loading} className="primary-btn"><Search size={18}/>{loading?'Finding safe routes…':'Find Safest Route'}</button></form> }
