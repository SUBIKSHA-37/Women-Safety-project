import { ShieldCheck, Building2, HeartPulse, Clock3 } from 'lucide-react';
export default function SafetyInfo({route,tracking}){if(!route)return <div className="info-card empty"><ShieldCheck/><p>Enter your journey to see route safety coverage.</p></div>; const mins=Math.round(route.duration_s/60); return <div className="info-card"><div className="flex items-start justify-between"><div><p className="eyebrow">Safety-weighted route</p><h2>Safety score <span className="text-emerald-600">{route.score}</span></h2></div><span className={'risk-pill '+route.risk_level}>{route.risk_level} risk</span></div><div className="stats"><div><Building2/><b>{route.police?.length || 0}</b><span>police</span></div><div><HeartPulse/><b>{route.hospitals?.length || 0}</b><span>hospitals</span></div><div><Clock3/><b>{mins}</b><span>minutes</span></div></div>{tracking&&<p className="safe-path"><ShieldCheck size={16}/> You are on a safe path</p>}<p className="mt-3 text-sm text-slate-600">{route.safety_explanation || 'Route chosen due to lower risk, better hospital coverage.'}</p></div>}
