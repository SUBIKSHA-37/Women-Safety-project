services:
  - type: web
    name: nila-backend
    env: docker
    branch: main
    dockerfilePath: backend/Dockerfile
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://your-frontend.vercel.app
